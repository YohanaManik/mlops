batch_size: 32
epochs: 50
learning_rate: 0.001
optimizer: "adam"
scheduler:
  type: "cosine"
  warmup_epochs: 5
early_stopping:
  patience: 7
  min_delta: 0.001
loss: "cross_entropy"
class_weights: null  # auto, null, or manual [1.0, 1.2, ...]
save_best_only: true